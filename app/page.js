import HeroCarousel from "@/components/HeroCarousel";

export default function Home() {
  return (
    <main className="w-full min-h-screen bg-zinc-50 dark:bg-black">
      <HeroCarousel />
    </main>
  );
}